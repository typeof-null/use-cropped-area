import { RefObject } from 'react';
export type Area = {
    x: number;
    y: number;
    width: number;
    height: number;
};
export declare const getCroppedArea: (image: HTMLImageElement, canvas: HTMLCanvasElement, area: Area, scale?: number) => void;
type Params = {
    area: Area;
    image: HTMLImageElement;
    visible?: boolean;
};
declare function useCroppedArea({ area, image, visible }: Params): RefObject<HTMLCanvasElement>;
export default useCroppedArea;
